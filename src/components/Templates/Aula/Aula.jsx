import React from 'react';
import Paginator from '../../Aula/Paginator/Paginator';

export default props => (
 
    <section>
        <header>
            <Paginator />
        </header>
    </section>
    
)